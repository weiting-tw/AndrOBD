<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application android:usesCleartextTraffic="true">

        <!-- Plugin discovery: host broadcasts IDENTIFY, this receiver responds
             in-process (same APK) so the MQTT plugin is built-in, no install. -->
        <receiver
            android:name="com.fr3ts0n.androbd.plugin.mqtt.PluginReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.fr3ts0n.androbd.plugin.IDENTIFY" />
                <category android:name="com.fr3ts0n.androbd.plugin.REQUEST" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.fr3ts0n.androbd.plugin.mqtt.MqttPlugin"
            android:exported="true"
            android:enabled="true"/>

        <!-- Config screen — launched by the plugin "configure" action, NOT a
             separate launcher icon (LAUNCHER intent-filter removed on bundling). -->
        <activity
            android:name="com.fr3ts0n.androbd.plugin.mqtt.SettingsActivity"
            android:exported="true"/>

    </application>

</manifest>
